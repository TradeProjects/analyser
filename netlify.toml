[build]
  command = "cd client && npm install --legacy-peer-deps && npm install @babel/plugin-proposal-private-property-in-object --save-dev && npm run build && cd .. && node server/server.jss"
  publish = "client/build"